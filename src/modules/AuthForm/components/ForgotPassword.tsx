import React from 'react'
import { CurrentForm } from '../AuthForm'

function ForgotPassword({setCurrent}: {setCurrent?: (val: CurrentForm) => void}) {
  return (
    <div>ForgotPassword</div>
  )
}

export default ForgotPassword